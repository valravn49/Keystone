[build]
builder = "NIXPACKS"
[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 200
[variables]
TZ = "Australia/Melbourne"
PORT = "8080"
DATABASE_URL = "sqlite:////data/sisters.db"
